"""Optional integrations with sibling Swarm Proof projects.

These lazily import their target library, so costbomb-core stays dependency-free;
install the matching extra (e.g. ``pip install costbomb[exactly-once]``) to activate.
"""
